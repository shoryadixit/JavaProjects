package JAVA;

public class ArrayDemo {
    private static final int NUMBER_OF_ROWS = 5;
    private static final int NUMBER_OF_COLUMNS = 5;

    public static void main(String[] args) {
        //IF we provide any one value in array like we give only insert 
        //the value of row so now we are called JAGGED ARRAY.
       /* Scanner sc = new Scanner(System.in);
        char[][] characters = new char[NUMBER_OF_ROWS][NUMBER_OF_COLUMNS];

        for (int i = 0; i < characters.length; i++) {
            for (int i1 = 0; i1 < characters.length; i1++) {
                System.out.printf("Please enter a character for (%d , %d)", i, i1);
                characters[i][i1] = sc.nextLine().charAt(0);
            }
        }

        for (char[] character : characters) {
            for (char characterValue : character) {
                System.out.print("'" + characterValue + "' ");
            }
            System.out.println();
        }
        //make a program to multiply two matrices.

        int[][] firstMatrix = new int[1][3];
        int[][] secondMatrix = new int[3][1];
        int[][] resultantMatrix = new int[1][1];

        //small task to practise the
        //make array in string type of 2d
*/
        String[][] commonFirstNamesInIndia = {
                /*Name Start with A*/{},
                /*Name Start with B*/{},
                /*Name Start with C*/{},
                /*Name Start with D*/{},
                /*Name Start with E*/{},
                /*Name Start with F*/{},
                /*Name Start with G*/{},
                /*Name Start with H*/{},
                /*Name Start with I*/{},
                /*Name Start with J*/{},
                /*Name Start with K*/{},
                /*Name Start with L*/{},
                /*Name Start with M*/{},
                /*Name Start with N*/{},
                /*Name Start with O*/{},
                /*Name Start with P*/{},
                /*Name Start with Q*/{},
                /*Name Start with R*/{},
                /*Name Start with S*/{},
                /*Name Start with T*/{},
                /*Name Start with U*/{},
                /*Name Start with V*/{},
                /*Name Start with W*/{},
                /*Name Start with X*/{},
                /*Name Start with Y*/{},
                /*Name Start with Z*/{},

        };

        //Another Demo
        //create a two - dimensional array to store the room
        //number of a hostel blocks

    }
}